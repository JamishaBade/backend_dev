Node.js is single threaded

- asynchronous opeartion

to exit event loop we need to do

```
process.exit()
```

- req: request object. Incoming requests from the client and holds info like the url, HTTP methods etc.
- res: response object. Used to send back response to the client.
